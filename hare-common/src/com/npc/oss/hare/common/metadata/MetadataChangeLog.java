/**
 * 
 */
package com.npc.oss.hare.common.metadata;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
/**
 * 元数据是否如此重要,需要通过持久化日志保护,还是可以容忍重新分配??
 * @author buhe
 *
 */
public class MetadataChangeLog {
	private final static Log LOG = LogFactory.getLog(MetadataChangeLog.class);
}
